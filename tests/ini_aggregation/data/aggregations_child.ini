# ==========================================
# groups section
# ==========================================
[bastion] # <=== [Override]
localhost router_id=10.x.x.x ssh_id=192.x.x.x

[node] # <=== [Override]
node-k8s-001 priority="100"

[master] # <=== [Override]
node-ms-001

[worker] # <=== [Override]
node-ap-001

[ceph-management] # <=== [Override]
node-ceph-001

[dummy]
localhost ansible_connection=local

# ==========================================
# aggregations section
# ==========================================
[k8s-nodes:children]
master
worker

[ceph:children]
ceph-management

# ==========================================
# global vars section
# ==========================================
# [k8s-nodes:vars]
# l4m_fs_size=200G
# lvm_img_size=200G
